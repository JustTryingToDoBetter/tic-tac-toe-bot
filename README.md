# 🎮 Tic-Tac-Toe (Python, Terminal Edition)

A simple **two-player Tic-Tac-Toe** game that runs in the terminal.  
Players alternate turns (`X` and `O`), the game detects wins and draws,  
and automatically starts a new match when a round finishes.

---

## ✨ Features
- Two-player mode (local, alternating turns)
- Input validation (no overwriting cells or invalid coordinates)
- Winner detection (rows, columns, diagonals)
- Draw detection when the board is full
- Auto-restarts after each match
- Scoreboard that persists during the session
- Alternates starting player each game for fairness
